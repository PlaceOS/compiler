name: compiler
version: 1.2.0

dependencies:
  driver:
    github: placeos/driver
    version: ~> 2.0

  exec_from:
    github: place-labs/exec_from

  rwlock:
    github: spider-gazelle/readers-writer
